# Backend-only dependencies (Flask API + MySQL). Kept slim on purpose —
# the scraper/data deps live in the repo-root requirements.txt and are not
# needed to run the API in production.
Flask==3.1.1
flask-cors==6.0.0
Flask-MySQLdb==2.0.0
mysqlclient==2.2.7
python-dotenv==1.1.0
gunicorn==23.0.0
